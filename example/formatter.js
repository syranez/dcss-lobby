const Stop   = require('./formatter/stop.js');
const Start  = require('./formatter/start.js');
const Update = require('./formatter/update.js');

module.exports = {
    Stop,
    Start,
    Update,
};
